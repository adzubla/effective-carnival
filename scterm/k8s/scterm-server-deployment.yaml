apiVersion: v1
kind: ConfigMap
metadata:
  name: scterm-server
data:
  application.properties: |-
    ibm.mq.connName=mq-deployment(1414)

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: scterm-server-deployment
spec:
  selector:
    matchLabels:
      app: scterm-server
  replicas: 1
  template:
    metadata:
      labels:
        app: scterm-server
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
        prometheus.io/scheme: "http"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: scterm-server
          image: localhost.localdomain:5000/scterm-server:1.0-SNAPSHOT
          imagePullPolicy: IfNotPresent
          env:
            - name: LICENSE
              value: accept
            - name: MQ_QMGR_NAME
              value: QM1

---
